/**
 * @module Calculator
 * */
export class Calculator {
    static Add = (a, b) => a + b;
    static Subtract = (a, b) => a - b;
    static Multiply = (a, b) => a * b;
    static Divide = (a, b) => a / b;
}
